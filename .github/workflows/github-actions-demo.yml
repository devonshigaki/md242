name: GitHub Actions Demo

on:
  push:
    branches:
      - main
      - md242

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Run a script
      run: echo "Hello, GitHub Actions!"

